pipeline {
    agent any
    environment {
        KUBECONFIG = credentials('secrets')
    }
    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'git@github.com:SaranyaSeethamraju07/Jenkins_Test.git', credentialsId: '0eecb35d-9e59-472c-9685-c0eb51b2168f'
            }
        }
        stage('Deploy to Kubernetes') {
            steps {
                // Add your deployment steps here
                powershell 'kubectl apply -f deployment.yaml --validate=false'
                powershell 'kubectl apply -f service.yaml --validate=false'
            }
        }
        stage('Test') {
            steps {
                // Add your testing steps here
                powershell 'kubectl get pods'
                powershell 'curl http://$(minikube service hello-app-service --url)'
            }
        }
    }
}
